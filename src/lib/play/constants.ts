/** Cookie name holding the participant team's session token on the shared phone. */
export const TEAM_COOKIE = "rally_team";

/** Point cost to buy/reveal the next photo in foto-navigatie. */
export const NEXT_STEP_COST = 5;
